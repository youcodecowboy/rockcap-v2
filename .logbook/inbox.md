# Logbook Inbox

> **⚠️ RETIRED 2026-07-18** — do not add new notes here. All open items below were migrated to the logbook MCP (LOG-12…LOG-26; see [MIGRATED.md](MIGRATED.md)). New notes go in as `create_item` calls on the `logbook` MCP server.

## Open (queued for separate work — MIGRATED, see banner)

- 2026-07-08 — **Naming standard V1.2 wiring** (client CONFIRMED the standard today; doc + filename_schema.json landed via PR #76). Remaining lane: (1) extend `parseDocumentName` (src/lib/documentNaming.ts) to the V1.2 grammar — dual-date names (two `\d{8}` tokens: document date after DocType pins vintage, trailing filing date is recency) + `R\d` terms-reissue tokens; (2) versionPrecedence series ordering should prefer document date over filing date when both present (out-of-order planning-permission case, standard §5); (3) reconcile `tentative:true` appFileType mappings in filename_schema.json against placement-rules.ts; (4) wire the parser into classification as a pre-LLM signal + generate standard-compliant names on ingest (drive.rename approval flow for legacy files). Sequence BEFORE the ~1000-doc batch so names carry classification signal. #naming #classification #drive

- 2026-07-06 — contact display-name hygiene (visible in knowledge graph): "Peter Mackenzie (175)" carries a numeric suffix in the contacts row; "Nicola Elia RenÃ¨ Kinnie" is UTF-8 mojibake (é double-encoded — likely from a CH/Apollo sync path). Audit contacts name fields; fix the two rows; find + fix the encoding culprit in the sync that wrote them. #backend #cleanup #data-quality

- 2026-07-06 — Mackenzie Miller Homes status anomaly (NOT a duplicate — full sweep of 257 client rows found exactly one MMH row, `kn7byatdbeywpd8z5c7aghf2m582jyvq`): fully intel-enriched, 3 active in-app projects (LEILAD/POOFL/TEMPG), yet still `status: prospect` / `prospectState: researched`. Operator believed a separate real client profile existed. Decide: promote to active (`client_activate`) so its document library shows in the Docs sidebar (which now filters prospects out), and generally audit clients with active projects still sitting in prospect status. #backend #cleanup #status

- 2026-07-06 — **Knowledge Layer Phase 3 — atom backfill for pre-Drive documents.** Once Spec 2 (atomization + entity graph) ships, existing documents already processed by the v4 bulk-upload pipeline must be reconciled into the atomic-fact model. Likely shape: a converter adapter that transforms stored v4 outputs (key terms, summaries, classifications) into provenance-carrying atoms without re-reading source bytes; fall back to full re-atomization only where v4 output is too thin. Scope: all documents ingested before the Drive backbone existed. Depends on: Spec 1 (drive ingestion) + Spec 2 (knowledge layer) both landed. #knowledge-layer #backfill #phase-3

- 2026-05-25 — frontend-vs-MCP divergence on `client.list`: MCP `client.list({status:"active"})` returns 9 rows; frontend reportedly shows 46 clients. Audit which is canonical (likely UI is including archived / past statuses or counting prospects too). Reconcile so a single query/filter rule governs both surfaces. Affects every Claude Code "tell me about X" workflow that relies on the MCP client list. #backend #cleanup #mcp-vs-ui
- 2026-05-25 — `projectIntelligence` rows are empty skeletons (version:1, lastUpdatedBy:"system", only keyParties.borrower populated) for all 3 sampled projects (Comberton, Manor Park Refi, Monksbury Court) — but the frontend reportedly shows rich project-level intel for these projects. Suggests the live intel content lives in a legacy table (probably `clientIntelligence`-style) that wasn't migrated when `projectIntelligence` was introduced. Decide canonical store + backfill / reconcile. Blocks deal-intake skill because the skill is supposed to write project intel and we need to know which row it should write into. #backend #cleanup #data-migration
- 2026-05-25 — duplicate `Kristian Hansen` client rows in the active list: two distinct IDs (`kn7cchj1mzw40tw1x59rbppxb9828j85` type=developer; `kn7ffesqdrp09ar2806dgtzy3s830jpe` type=borrower). Likely test artefacts but worth confirming + deduping. Pollutes any "active clients count" + similar aggregates. #backend #cleanup #dedupe
- 2026-05-25 — `fileTypeDetected` missing types: `Brochure` (sales brochures — Manor Park has `ManorParkBrchMar25-FINAL-Digital.pdf` classified as `Other Document`), `CV` / `Director Profile` (Monksbury's `Director's CVs.docx` was classified `Email/Correspondence` wrongly), `Bridge Loan Application` (would help bridging-type detection). Add to V4 vocabulary (`src/lib/references/references/*.ts`). Pair with the V4 prompt-side normalisation rules from triage items #5/#6/#7 (already documented in deal-intake vocabulary catalogue; V4 prompt edits land separately to avoid retriggering classification on existing docs). #backend #classifier #vocab
- 2026-05-25 — multi-entity SPV ownership structure is not captured in schema. Real production deals have 5+ entities per deal (Sponsor → Borrower SPV → Lender SPV → Lender → Agent → Personal Guarantors). System's flat `clientRoles` model only stores 1-2 (typically just the parent borrower). Two paths: (A) extend `clientRoles` schema to support nested SPV chains; (B) formalise capture via `knowledgeItems` at `borrower.spvStructure` with a structured value shape. Path B is lower-risk; path A is more queryable. Decide. Deal-intake skill is using path B for now. #backend #schema #spv-structure
- 2026-05-25 — scheme aliasing: schemes have multiple names in real production (Comberton scheme = Bennel Farm site = Mendlesham some-docs-reference). Plus portfolio docs cover multiple schemes (Bayfield Portfolio appraisal covers 4 schemes). Capture via `knowledgeItems` at `scheme.aliases` (decision per operator 2026-05-25). May want a `schemeAliases` indexed view later if queries become common. #knowledge-items #ux
- 2026-05-25 — **BUG surfaced live during Sprint I test fire**: `approvals.create` mutation requires Clerk auth (`getAuthenticatedUser`) and the `ENTITY_TYPE` enum doesn't include `document_classification_audit` — both block the deal-intake skill from staging audit corrections via approvals. Two fixes needed: (A) add `internal.approvals.createInternal` that takes `userId` as arg (same pattern as Sprint G tasks.createInternal); (B) extend `ENTITY_TYPE` enum to include `document_classification_audit` + possibly other skill-emitted types. Until both ship, deal-intake step 9 can't stage corrections through the standard approvals queue — falls back to a `knowledgeItems` row at `dealIntake.auditCorrections` (deferred persistence). #backend #substrate #approvals
- 2026-05-25 — substrate gap: no MCP-exposed mutation for the deal-intake skill to advance project `lifecycleStage` (e.g., from "prospective" to "active"). Currently the only mutation that touches projects directly is `projects.create`. Need either (A) `project.update({projectId, lifecycleStage, ...})` MCP wrapper around an existing `projects.update`-style mutation, OR (B) a focused `project.advanceLifecycleStage` tool. Used by deal-intake when phase detection promotes a deal from indicative_terms → credit_submission → monitoring. #backend #substrate
- 2026-05-25 — `tasks.createInternal` requires `userId: v.id("users")` (NOT optional) — but skill contexts may not have an explicit user (e.g., automated skill runs after webhook firing, OR test environments with 0 users). Make `userId` optional; when absent, leave `assignedTo` empty (unassigned task) OR fall back to a system-owner user (decide). Same issue likely applies to other Sprint G internal variants. #backend #substrate

## Fixed in PR #15 (Sprint I — landed 2026-05-25)

- ✅ FIXED 2026-05-25 — `documents.update` folder-validation bug at convex/documents.ts:776. Validator now only runs when caller is explicitly changing folder fields (`folderId` / `folderType` / `clientId` / `projectId` present in the update args). Grandfathered for unchanged folder state. Sprint H workaround (`folderId: null, folderType: null` in correction calls) no longer required; removed from corrections corpus + audit playbook + SKILL.md "What goes wrong." Original /jot text: documents.update mutation validates folderId even when it is NOT being changed in the update args [...] Hit on 2 of 5 corpus corrections.
- ✅ FIXED 2026-05-25 — `._*` macOS resource-fork upload filter. Added rejection at `bulkUpload.addItemToBatch` (throws `rejected_macos_resource_fork: <filename>`). 0-byte sidecar files now blocked at intake before V4 runs. Deal-intake skill's resource-fork check (step 4) is now defence-in-depth for historical docs. Original /jot text: `._*.pdf` / `._*.xlsx` macOS resource-fork files are being ingested [...] 10+ noise files in Manor Park Refi alone.
- ✅ FIXED (doc-only) 2026-05-25 — `fileTypeDetected` Roof Plan vs Roof Plans drift. Operator-confirmed canonical: **`Roof Plans`** (plural to match Floor Plans / Site Plans). Documented in `skills/skills/deal-intake/references/document-vocabulary-catalogue.md` with directive normalisation rule. Skill normalises in-memory on read. V4 prompt-side fix pending (separate PR with vocab additions).
- ✅ FIXED (doc-only) 2026-05-25 — `fileTypeDetected` Other / Other Document / Unclassified consolidation. Operator-confirmed canonical: **`Unclassified`**. Same rule applied to category-level drift (Other / Miscellaneous / Unclassified → `Unclassified`). Documented in vocab catalogue + directive normalisation rule.
- ✅ FIXED (doc-only) 2026-05-25 — `category` Legal vs Legal Documents drift. Operator-confirmed canonical: **`Legal Documents`** (matches 13-category canon from MEMORY.md). Documented in vocab catalogue + directive normalisation rule.
- ✅ DONE — corrections corpus design shipped: `skills/corpora/document-classification-corrections.md` created with 5 entries from live fixes during research + 9 diagnostic patterns + skill usage guide. Loaded by deal-intake at startup for pre-validation; future-consumed by classification-critic.

- 2026-07-07 — Email overhaul SPEC'd (docs/spec-3-email-overhaul.md, recon-grounded): headline bugs F1 (email_fresh approvals never send — silent stub, marked executed) + F2 (only mailbox's sendEnabled OFF — nothing sends today, operator can flip at /settings/gmail); HubSpot race → contentless duplicate replies; contact identity gaps; countUnrouted broken in prod (16MB). Verdict: keep Gmail transport, NO Resend. W0-W4 workstreams; W4 = email→atoms (spec 2 §14c). HAND-OFF: another session builds this.
- [2026-07-11] Knowledge: move atom embeddings to a side table (atomEmbeddings) — graph reads pay ~8KB/row for unused vectors; expandEntity scan caps could relax 10x. See PR #90 comments.
