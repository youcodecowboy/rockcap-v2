# Logbook Inbox

- 2026-05-25 — frontend-vs-MCP divergence on `client.list`: MCP `client.list({status:"active"})` returns 9 rows; frontend reportedly shows 46 clients. Audit which is canonical (likely UI is including archived / past statuses or counting prospects too). Reconcile so a single query/filter rule governs both surfaces. Affects every Claude Code "tell me about X" workflow that relies on the MCP client list. #backend #cleanup #mcp-vs-ui
- 2026-05-25 — `projectIntelligence` rows are empty skeletons (version:1, lastUpdatedBy:"system", only keyParties.borrower populated) for all 3 sampled projects (Comberton, Manor Park Refi, Monksbury Court) — but the frontend reportedly shows rich project-level intel for these projects. Suggests the live intel content lives in a legacy table (probably `clientIntelligence`-style) that wasn't migrated when `projectIntelligence` was introduced. Decide canonical store + backfill / reconcile. Blocks deal-intake skill because the skill is supposed to write project intel and we need to know which row it should write into. #backend #cleanup #data-migration
- 2026-05-25 — duplicate `Kristian Hansen` client rows in the active list: two distinct IDs (`kn7cchj1mzw40tw1x59rbppxb9828j85` type=developer; `kn7ffesqdrp09ar2806dgtzy3s830jpe` type=borrower). Likely test artefacts but worth confirming + deduping. Pollutes any "active clients count" + similar aggregates. #backend #cleanup #dedupe
- 2026-05-25 — `._*.pdf` / `._*.xlsx` macOS resource-fork files are being ingested by the upload pipeline + run through V4 classifier as if real docs. 10+ noise files in Manor Park Refi alone (Statement, Application Form, Sales Schedule duplicates). Add an upload-time filter that rejects filenames starting with `._` before they hit V4. Pollutes doc counts, checklist fulfilment accuracy, and burns classifier tokens. #backend #cleanup #upload-filter
- 2026-05-25 — `fileTypeDetected` vocabulary drift: `Roof Plan` vs `Roof Plans` (singular/plural). Normalise to one canonical (recommend plural `Roof Plans` to match `Floor Plans`/`Site Plans` pattern). Add to V4 prompt as a normalisation rule. #backend #cleanup #vocab
- 2026-05-25 — `fileTypeDetected` defeat-state has 3 names: `Other`, `Other Document`, `Unclassified`. Pick one canonical (recommend `Unclassified` as most descriptive). Same drift exists for category: `Other` + `Miscellaneous` + `Unclassified` all mean "don't know" — consolidate. #backend #cleanup #vocab
- 2026-05-25 — `category` drift: `Legal` vs `Legal Documents` used inconsistently for the same kind of doc. Normalise to `Legal Documents`. Similarly `Marketing & Sales` vs `Marketing Materials` vs `Project Documents` overlap (a sales brochure could land in any of the 3). Define canonical use. #backend #cleanup #vocab
- 2026-05-25 — `fileTypeDetected` missing types: `Brochure` (sales brochures — Manor Park has `ManorParkBrchMar25-FINAL-Digital.pdf` classified as `Other Document`), `CV` / `Director Profile` (Monksbury's `Director's CVs.docx` was classified `Email/Correspondence` wrongly), `Bridge Loan Application` (would help bridging-type detection). Add to V4 vocabulary. #backend #classifier #vocab
- 2026-05-25 — multi-entity SPV ownership structure is not captured in schema. Real production deals have 5+ entities per deal (Sponsor → Borrower SPV → Lender SPV → Lender → Agent → Personal Guarantors). System's flat `clientRoles` model only stores 1-2 (typically just the parent borrower). Two paths: (A) extend `clientRoles` schema to support nested SPV chains; (B) formalise capture via `knowledgeItems` at `borrower.spvStructure` with a structured value shape. Path B is lower-risk; path A is more queryable. Decide. Deal-intake skill is using path B for now. #backend #schema #spv-structure
- 2026-05-25 — scheme aliasing: schemes have multiple names in real production (Comberton scheme = Bennel Farm site = Mendlesham some-docs-reference). Plus portfolio docs cover multiple schemes (Bayfield Portfolio appraisal covers 4 schemes). Capture via `knowledgeItems` at `scheme.aliases` (decision per operator 2026-05-25). May want a `schemeAliases` indexed view later if queries become common. #knowledge-items #ux
- 2026-05-25 — corrections corpus design: per operator directive, every approved misclassification fix should append to `skills/corpora/document-classification-corrections.md` so the classifier improves over time. Consumers: (1) deal-intake skill pre-validates V4 output against the corpus, (2) classification-critic skill (skeleton) ingests for vocabulary/prompt improvement proposals, (3) future V4 enhancement loads as in-context examples. Start the corpus as part of deal-intake hardening PR. #skill-design #classifier-improvement #corpus
- 2026-05-25 — **BUG surfaced live in Sprint H test**: `documents.update` mutation in convex/documents.ts:776 validates `folderId` even when it is NOT being changed in the update args. If the existing record has an invalid `folderId` (e.g., V4 classifier wrote `folderId: "kyc"` on a project-scoped doc, but kyc is a client-level folder name that doesn't exist for the project), ANY update to that doc fails until the operator also passes `folderId: null, folderType: null` to clear the bad folder. Hit on 2 of 5 corpus corrections (Capstone Quinn Track Record had folderId=kyc; John Parker ALIE had folderId=miscellaneous). Two fixes possible: (A) make validator skip if folderId not in update args (use existing.folderId as-is, even if invalid); (B) make validator grandfather invalid existing folders. (A) is simpler. Until fixed, all classification-correction tools need a workaround. #backend #bug #sprint-h-substrate
