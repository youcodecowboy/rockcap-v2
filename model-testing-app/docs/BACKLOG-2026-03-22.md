# Client Feedback Backlog — 2026-03-22

> **Source:** Bill of Work — Client Feedback Backlog (March 22, 2026)
> **Status tracking:** Mark items `[x]` when complete. Add branch/commit info.

---

## Execution Order

### Tier 1 — Quick Wins (Agent: Main) Checked

- [x] **UIX-01** | Fix white text on white background for user names and projects
  - Priority: High | Effort: Low | Category: UI/UX
  - **Commit:** `30faf8d`
  - **Root cause:** The `@` mention autocomplete dropdowns (`NoteMentionList.tsx` and `MentionAutocomplete.tsx`) had no explicit `color` set. The containers and buttons relied on inherited text color. Because tippy.js portals append to `document.body`, and Tailwind v4 CSS layers can cause unexpected color inheritance, the text inherited `color: white` from parent contexts (e.g., the Notes page header with `bg-blue-600 text-white`).
  - **Fix:** Added explicit `text-gray-900 dark:text-zinc-100` to both the dropdown container `<div>` and each `<button>` element in `NoteMentionList.tsx` (Notes TipTap editor) and `MentionAutocomplete.tsx` (Chat input). This ensures mention autocomplete text is always readable regardless of portal parent context.

- [x] **UIX-02** | Remove red trash can icon from line items 
  - Priority: Low | Effort: Low | Category: UI/UX
  - **Commit:** `b43aaf6`
  - **Root cause:** Task line items in `ClientTasksTab.tsx` and `ProjectTasksTab.tsx` had a hover-revealed red `Trash2` icon button (`opacity-0 group-hover:opacity-100`) positioned absolutely in the top-right corner of each task row.
  - **Fix:** Removed the entire `<button>` with the red Trash2 icon from the `TaskListItem` component in both files. The `group` class on the parent div was also removed (no longer needed). Delete functionality remains available through the task detail panel when a task is selected. Cleaned up the unused `onDelete` destructured prop with a `_` prefix.

- [x] **LIB-03** | Fix 'View All' in recent documents to navigate to document library
  - Priority: Medium | Effort: Low | Category: Doc Library
  - **Root cause:** The "View All" buttons in `ProjectOverviewTab.tsx` and `ClientOverviewTab.tsx` used `router.push()` to navigate to `?tab=documents`. However, since the page component was already mounted, `useState(initialTab)` only read the URL param once on mount. Subsequent `router.push` URL changes didn't update the `activeTab` React state, so the tab never switched.
  - **Fix:** Added an `onTabChange` callback prop to both `ProjectOverviewTab` and `ClientOverviewTab`. The parent pages (`[projectId]/page.tsx` and `[clientId]/page.tsx`) pass their existing `handleTabChange` function which updates both the React state AND the URL. All "View All" buttons now call `onTabChange?.('documents')` / `onTabChange?.('checklist')` / `onTabChange?.('tasks')` / `onTabChange?.('projects')` instead of `router.push`.

- [x] **FIL-01** | Fix download filename preservation
  - Priority: High | Effort: Low | Category: File Mgmt
  - **Root cause:** The document reader page (`docs/reader/[documentId]/page.tsx`) and `FileTypeDefinitionView.tsx` used `<a href=convexUrl download=fileName>` to trigger downloads. However, Convex storage URLs are cross-origin, and browsers ignore the HTML `download` attribute for cross-origin URLs — falling back to the URL's last path segment (a Convex storage ID like `kg27abc123...`) as the filename.
  - **Fix:** Enhanced the existing `/api/convex-file` proxy route to accept a `filename` query param and set a `Content-Disposition: attachment; filename="..."` header. The response body is now streamed through (`response.body` passthrough) instead of buffered as a blob, avoiding memory issues with large files. Updated the reader page and FileTypeDefinitionView download handlers to use `window.open('/api/convex-file?storageId=...&filename=...', '_blank')`, letting the browser handle the download natively with the correct filename via the server-set header.

---

### Tier 2 — Classification Pipeline Fixes (Agent: Separate)

> **IMPORTANT:** Execute CLS-01 and CLS-03 before CLS-02. The migration script (CLS-02) should run after the mapping logic is fixed so newly-filed docs are correct.

- [x] **CLS-01** | Fix folder mapping logic: appraisals only in Appraisal folder
  - Priority: Critical | Effort: Medium | Category: Classification
  - **Solution:** Updated `CATEGORY_PLACEMENT` in `src/v4/lib/placement-rules.ts` so only the "Appraisals" category routes to the `appraisals` folder. Professional Reports, Plans, Photographs, Project Documents, and Warranties now route to `background` (project-level). Added `FILE_TYPE_OVERRIDES` for Accommodation Schedule and Build Programme as deterministic safety nets. Synced `CATEGORY_TO_FOLDER_MAP` in `convex/folderStructure.ts` and the mock client in `src/v4/lib/mock-client.ts` to match. Also fixed mock client bugs: Inspections was mapping to `operational_model` (should be `post_completion`), Communications was mapping to client-level (should be project-level).

- [x] **CLS-03** | Fix classification: background docs not categorized as KYC
  - Priority: Critical | Effort: Medium | Category: Classification
  - **Solution:** Two-layer fix. (1) Deterministic: `FILE_TYPE_OVERRIDES` in `placement-rules.ts` ensure Accommodation Schedule and Build Programme always route to `background` regardless of model classification. (2) LLM guidance: Added KYC-negative disambiguation rules to Accommodation Schedule and Build Programme references in `src/lib/references/references/project-documents.ts` ("NOT a KYC document"). Added property-document-negative rule to Certificate of Incorporation in `src/lib/references/references/kyc.ts` warning against classifying construction/property docs as KYC.

- [x] **CLS-02** | Migration script: move non-appraisals out of Appraisal folders
  - Priority: Critical | Effort: Medium | Category: Classification
  - **Solution:** Created `convex/migrations.ts` with two functions: `previewAppraisalMigration` (read-only query for dry runs) and `migrateAppraisalFolder` (mutation). Both accept optional `projectId` for per-project scoping. Queries documents where `folderId === "appraisals"` and `folderType === "project"`, moves any non-Appraisals-category doc to `background`. Handles edge cases: skips docs with no category, creates missing background folders. Tested on Bayfield Homes / Comberton first (19 Plans + 1 Professional Report moved, 5 Appraisals kept). Then ran across all projects — 328 remaining docs scanned, 0 additional moves needed (all correctly filed).
  - **Executed:** 2026-03-22

- [x] **CLS-04** | Fix .EML file classification: classify by content, not file type
  - Priority: High | Effort: High | Category: Classification
  - **Solution:** Three-layer fix to remove email-format bias. (1) `src/v4/lib/document-preprocessor.ts`: Strip `.eml`/`.msg` extensions before `analyzeFilename()` so filename hints don't suggest Communications. (2) `src/lib/references/references/communications.ts`: Removed `eml$` and `msg$` from `filenamePatterns` and removed the `.eml`/`.msg` extension boost from `decisionRules` so the tag resolver doesn't score toward Email/Correspondence on file extension alone. (3) `src/v4/lib/anthropic-client.ts`: Added per-document annotation in `buildBatchUserMessage()` for `.eml`/`.msg` files instructing the model to classify by the actual document content, not the email container. Existing system prompt instruction retained as reinforcement.

---

### Tier 3 — Medium Features & UX

- [x] **UIX-03** | Sort clients list by most recently accessed ✅
  - Priority: Medium | Effort: Low | Category: UI/UX
  - Root clients list currently sorts alphabetically
  - Change to sort by most recently clicked/accessed so active clients appear at the top
  - **Solution:** Added `lastAccessedAt` field + index to clients schema. New `recordAccess` mutation with 30s debounce fires on client click. `ClientsSidebar` sorts by recency descending with alphabetical fallback.
  - **Commits:** `cc8f47c`, `b2fc874`
  - **Files:** `convex/schema.ts`, `convex/clients.ts`, `src/app/clients/components/ClientsSidebar.tsx`

- [x] **FIL-03** | Duplicate file functionality ✅
  - Priority: Medium | Effort: Low | Category: File Mgmt
  - Add 'Duplicate' option to the three-dot context menu on files
  - Duplicated file should be flagged as a duplicate in the notes section
  - **Solution:** New `duplicateDocument` mutation clones the document record referencing the same `fileStorageId` (no blob duplication). Generates "(Copy)" name and new document code. Flagged via `versionNote`. "Duplicate" menu item added to FileCard context menu after "Move to Folder".
  - **Commits:** `7946c35`, `da80362`
  - **Files:** `convex/documents.ts`, `src/app/docs/components/FileCard.tsx`, `src/app/docs/components/FileList.tsx`

- [x] **NOT-02** | Add Notes tab to document preview drawer ✅
  - Priority: High | Effort: Medium | Category: Notes / AI
  - In the document preview (before opening full reader), add a 'Notes' tab
  - Place after the Checklist tab
  - Increase the preview drawer width by approximately 35% to accommodate the extra tab
  - Eliminates the need to click into the full document reader just to view notes
  - **Solution:** Extracted shared `DocumentNotes` component reusing existing `DocumentNoteForm` + `DocumentNoteCard`. Added 6th "Notes" tab to `FileDetailPanel` after Checklist. Drawer widened from 1080px to 1460px (~35%). "Intelligence" shortened to "Intel" for tab fit.
  - **Commits:** `5f4ee63`, `7235c3f`
  - **Files:** `src/components/DocumentNotes.tsx` (new), `src/app/docs/components/FileDetailPanel.tsx`

- [x] **NOT-01** | AI-powered note cleanup via text highlight ✅
  - Priority: Medium | Effort: Medium | Category: Notes / AI
  - In the Notes editor, allow users to highlight text and trigger an AI cleanup
  - Use Claude Haiku via existing Anthropic API token/integration
  - Cleans up grammar, formatting, and clarity of the selected text
  - **Solution:** New `/api/note-cleanup` route using Claude Haiku 4.5 with system prompt tuned for "enhance, don't rewrite" (fixes grammar/formatting, adds clarity/substance, preserves user's voice). Floating `NoteCleanupBubble` appears on text selection (5+ chars) with "Clean up" button. Undo toast after cleanup.
  - **Known limitation:** Cleanup modifies DOM visually but does not yet persist to Convex — needs follow-up to wire the `updateNote` mutation.
  - **Commits:** `4a26a2e`, `8160348`
  - **Files:** `src/app/api/note-cleanup/route.ts` (new), `src/components/NoteCleanupBubble.tsx` (new), `src/components/DocumentNotes.tsx`

- [x] **LIB-02** | Drag-and-drop file movement between folders ✅
  - Priority: Medium | Effort: Medium | Category: Doc Library
  - Enable drag-and-drop in the document library to move files between folders
  - Example: drag a document from Appraisals folder into Background folder
  - Replaces the current click > Move > Select Folder workflow
  - **Solution:** Files made draggable with multi-select awareness (dragging a selected file drags all selected). Custom MIME type `application/x-document-ids` carries doc IDs. All 4 folder rendering paths in `FolderBrowser` accept drops with amber dashed ring indicator. Uses existing `moveDocument`/`bulkMove` mutations. Toast with undo on success.
  - **Commits:** `19befb6`, `40f4aa0`
  - **Files:** `src/app/docs/components/FileCard.tsx`, `src/app/docs/components/FileList.tsx`, `src/app/docs/components/FolderBrowser.tsx`
  - **Shared utility:** `src/components/UndoToast.tsx` (`caa277d`) — reusable toast-with-undo used by FIL-03, NOT-01, LIB-02

---

### Tier 4 — Major Features (Require Planning)

- [ ] **SAF-01** | Soft delete with recovery for projects and clients
  - Priority: Critical | Effort: High | Category: Safety
  - Implement soft delete: deleted projects/clients are recoverable for a set period
  - Show a notification when something is deleted with a recovery option
  - Move delete functionality away from the top-right corner (too easy to click accidentally)
  - Relocate delete to Settings at the project/client level
  - Require a confirmation modal before any delete action

- [ ] **LIB-01** | Project-level document library settings with custom fields
  - Priority: High | Effort: High | Category: Doc Library
  - Add a Settings panel to the document library at the project level
  - Allow configuration of short codes and custom naming fields
  - Custom field changes cascade through the project
  - Custom fields can remain empty by default and be user-populated

- [ ] **FIL-02** | Improve automatic file naming flexibility
  - Priority: High | Effort: High | Category: File Mgmt
  - Current auto-naming works but lacks flexibility
  - Allow users to add custom fields to file names via the three-dot menu > Rename
  - Custom fields should be configurable at the project level in Settings
  - **Depends on:** LIB-01 (project-level settings)

---

## Architecture Notes for Agents

- **Stack:** Next.js 16 + Convex backend + Anthropic Claude API
- **Classification pipeline:** V4 at `src/v4/`, API route at `src/app/api/v4-analyze/`
- **Shared reference library:** `src/lib/references/` — tag-based resolver for document classification
- **Filing flow:** `src/lib/bulkQueueProcessor.ts` → `/api/v4-analyze`
- **Chat system:** Claude Haiku 4.5 via `@anthropic-ai/sdk`, tools in `src/lib/tools/`
- **Build check:** Always run `npx next build` before committing
- **Commit rule:** Push to GitHub after each change for code review ease
