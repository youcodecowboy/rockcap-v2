"use client";
// v1.2 stub — diff view in v1.2.1
export function RevisionDiffView({ original, revised }: { original: any[]; revised: any[] }) {
  return <div style={{ padding: 16, color: "#6b6b6b", fontSize: 11 }}>Diff view lands in v1.2.1.</div>;
}
