// =============================================================================
// CLASSIFICATION AGENT TYPES
// =============================================================================

// Re-export from main types for convenience
export type {
  ClassificationDecision,
  DocumentSummary,
  FolderInfo,
  FileTypeDefinition,
  FilenameTypeHint,
} from '../types';
