# Cursor Rules

## Changelog Management

The changelog is accessible at `/settings/changelog` in the application. Entries can be added manually through the UI on that page. When entries are added, all users automatically receive notifications.

